import 'package:test/test.dart';

import '../lib/src/authentication/cra_authentication.dart';
import '../lib/src/authentication/session.dart';

void main() {
  group('Authenticate', () {
    /*Session.start(
      authId: "Richi",
      realm: "de.konsultaner",
      transport: MockTransport("ssl","localhost",8080),
      authMethods: [CraAuthentication("password")],
      reconnect: Duration(seconds: 5)
    ).listen((session) {

    });*/
  });
}