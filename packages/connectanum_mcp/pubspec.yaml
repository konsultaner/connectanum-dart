name: connectanum_mcp
description: Model Context Protocol server primitives for Connectanum apps.
version: 0.1.0
publish_to: none
environment:
  sdk: '^3.9.2'
dependencies:
  connectanum_client: ^2.2.6
  meta: ^1.16.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
resolution: workspace
