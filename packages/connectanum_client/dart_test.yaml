platforms: [vm, chrome]
compilers: [dart2wasm]
on_platform:
  chrome: {timeout: 10x}

override_platforms:
  chrome:
    settings:
      executable:
        linux: chromium-browser
        mac_os: Chromium
      arguments: --no-sandbox
