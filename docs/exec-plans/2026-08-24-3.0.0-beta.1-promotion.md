# 3.0.0-beta.1 Promotion

Status: active
Started: 2026-08-24

## Goal

Promote the completed router-hosted MCP, asynchronous SCRAM, and multi-gigabit
transport work to the audited GitHub `master` branch as one synchronized
`3.0.0-beta.1` release candidate for consumer application testing.

## Release Contract

- All seven Dart packages and three Rust crates share `3.0.0-beta.1`.
- Public workspace dependencies use `^3.0.0-beta.1`.
- Runtime MCP identities, changelogs, installation guidance, and release gates
  expose the same beta version.
- Full local verification and strict package dry-runs pass before promotion.
- GitHub `master` is the audited release branch. GitLab `master` moves only by
  fast-forward.
- Package publish tags remain operator-gated and are not implied by pushing
  `master`.

## Work

1. Synchronize package, crate, dependency, runtime, changelog, and release-tool
   version surfaces at `3.0.0-beta.1`.
2. Run focused release validation, strict package dry-runs, and `bin/verify`.
3. Commit the completed implementation and release metadata, then push the
   feature branch.
4. Fast-forward eligible `master` refs and push GitHub `master`.
5. Watch exact-head deployment workflows and run the strict deployment audit.

## Progress

- 2026-08-24: User approved promotion to `master` for downstream beta testing.
- 2026-08-24: Pre-promotion `bin/verify` passed on the complete implementation
  worktree, including Rust, Dart, package, consumer, live WAMP/MCP/auth, remote
  auth, and Chrome Dart2Wasm coverage.
- 2026-08-24: Coordinated beta commit `094340c4` was pushed to the feature
  branch and fast-forwarded to GitHub and GitLab `master`; no package tag or
  public package release was created.
- 2026-08-24: Exact-head package dry-run, kTLS validation, and five-platform
  native artifact dry-run passed. Heavy WAMP diagnostics failed in the 4 MiB
  MessagePack WebSocket pub/sub workload after borrowed native `CHALLENGE`
  metadata lost its allocation anchor during optimized session
  materialization.
- 2026-08-24: The corrective client patch moves native session-message anchors
  to a shared registry and transfers the anchor at every
  `NativeSessionMessage.materialize()` boundary without copying payload bytes.
  Focused binding, client, and native transport suites pass, local companion
  review has no confirmed blocker, and full `bin/verify` passes with exit code
  0. Commit `ee7149ab` is on both feature and `master` refs; exact-head package
  and router image dry-runs pass.
- 2026-08-24: Hosted WAMP benchmarks exposed a native consuming-E2EE accessor
  regression after successful receipt: public lazy semantic getters returned
  the still-encoded outer WAMP arguments. Event, result, and invocation
  wrappers now materialize and cache plaintext on first semantic access while
  preserving lazy encoded byte getters. Benchmark matcher failures propagate
  immediately without losing an event. Focused unit and real-router
  mixed-serializer XSalsa/AES-GCM tests pass, as do the exact four-row E2EE
  smoke, artifact gate, and `bin/test-fast`. Full `bin/verify`, corrective
  promotion, exact-head WAMP reruns, and the strict deployment audit remain.
- 2026-08-24: A full `bin/verify` attempt passed formatting, Rust core/FFI,
  Dart core, MCP, and facade suites, then exhausted all three guarded
  fresh-cache MCP consumer dependency-resolution attempts while direct
  `pub.dev` access was timing out. Hosted exact-head verification must supply
  the remaining clean-network evidence.
- 2026-08-24: The hosted 24-row WAMP diagnostic matrix completed; only four WSS
  rows missed the strict 2 Gbit/s throughput budget. Those performance findings
  remain open and are separate from the E2EE correctness fix.
- 2026-08-24: Corrective commit `fe562ea1` is on both feature and `master` refs
  on GitHub and GitLab. Its package dry-run passes. The hosted WAMP run completed
  all workloads and reached the artifact gate, proving the E2EE accessor fix;
  all throughput floors pass, while three pure-Dart AES-GCM p95 rows exceed
  their latency ceilings despite equal-or-better total elapsed time and
  throughput than the last green hosted run. The exact rerun reproduced only
  two pub/sub tail findings while improving aggregate throughput and average
  latency. Hosted-calibrated Dart AES-GCM ceilings are now 5000 ms for RPC and
  6500 ms for pub/sub; throughput floors plus every native and XSalsa ceiling
  remain unchanged.
- 2026-08-24: Hosted Full Verify isolated a separate Chrome startup failure to
  the Ubuntu runner's unavailable sandbox. The canonical Chrome helper now
  launches the selected browser through a stable wrapper and adds
  `--no-sandbox` only on Linux CI. Verification-script regressions and the exact
  core SCRAM Worker and client WebSocket Chrome Dart2Js suites pass locally. A
  first full verification attempt had one non-reproducing HTTP/3 MCP handshake
  timeout; the exact case, its full 76-test native integration file, and a clean
  second `bin/verify` all pass. The corrective CI commit, hosted reruns, and
  strict audit remain.
