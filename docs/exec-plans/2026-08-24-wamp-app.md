# WampApp Consumer Application

Status: active
Started: 2026-08-24

## Goal

Build a production-oriented Flutter messaging example that proves the public
Connectanum packages can support account onboarding, WAMP-SCRAM, end-to-end
encrypted messaging, offline operation, rich media, calling, backup, and MCP
integration without private project assumptions.

## Architecture Contract

- Keep `examples/wamp_app/client`, `server`, and `shared` standalone so Flutter
  does not enter the root Dart workspace or package release graph.
- Consume synchronized hosted Connectanum prereleases rather than local package
  paths; only the WampApp protocol package is a local path dependency.
- Use one WebSocket endpoint with an anonymous registration realm and a
  separately authenticated application realm.
- Use standardized `wamp-scram` on the wire and Argon2id13 verifier material at
  rest. Never persist or log plaintext passwords, proofs, salts, keys, or worker
  messages beyond their required lifetime.
- Treat the router and application service as untrusted for message contents.
  Durable messages and attachments use application-level encrypted envelopes in
  WAMP binary fields; only clients hold conversation plaintext keys.
- Use WAMP for control, RPC, pub/sub, offline synchronization, attachment
  coordination, WebRTC signaling, and MCP. Use WebRTC/SRTP for call media.
- Do not present unimplemented capabilities as working UI or seed fake chats.

## Milestones

1. **Account vertical slice:** standalone packages, YAML server, anonymous
   registration, asynchronous Argon2id13 derivation, SCRAM login, responsive
   Flutter shell, verifier-only persistence, and consumer integration smoke.
2. **Identity and local trust:** per-device identity keys, encrypted local
   database, device enrollment/revocation, contact identity verification, and
   conversation-key wrapping.
3. **Durable messaging:** opaque server mailbox, receipts, reconnect cursors,
   one-to-one and group chats, read state, retry/deduplication, expiration, and
   one-time message semantics.
4. **Rich content:** encrypted attachments, images, GIFs, stickers, emoji,
   voice notes, bounded file transfer, resumability, and transfer benchmarks.
5. **User experience:** profiles, status, contacts import, global search,
   read/unread filters, themes, per-chat mute, and platform push notifications.
6. **Backup and recovery:** encrypted local export/import plus provider-neutral
   cloud backup with explicit key recovery and corruption tests.
7. **Calling:** WebRTC voice/video sessions with WAMP signaling, TURN guidance,
   call-state recovery, and mobile/web permission handling.
8. **MCP:** authenticated server tools/resources/prompts and client-side user
   consent boundaries for AI-assisted actions; no plaintext chat export by
   default.
9. **Production evidence:** threat model, abuse/rate limits, migrations,
   multi-device conflict tests, browser/mobile smokes, packaging, benchmarks,
   and full repository verification.

## Progress

- 2026-08-24: Milestone 1 implementation is complete locally. Public
  `3.0.0-beta.2` dependencies resolve for the standalone server and Flutter
  client. Shared, server, and client analysis/tests pass.
- 2026-08-24: The consumer integration test exposed and fixed two configuration
  boundaries: credential providers must be registered inside router worker
  isolates, and SCRAM must be advertised as standardized `wamp-scram` rather
  than the authenticator implementation shorthand `scram`.
- 2026-08-24: End-to-end registration and SCRAM reconnect pass through the
  native router, including server-signature verification and verifier-only
  account persistence.
- 2026-08-24: A real Flutter web browser smoke completed account registration
  and SCRAM login while the 64 MiB Argon2id13 worker ran without blocking UI
  progress. The authenticated shell rendered cleanly at desktop and 390-pixel
  widths with no browser console errors.
- 2026-08-24: Shared analysis and 5 tests, server analysis and 6 tests, Flutter
  analysis and 6 tests, and repository-wide `bin/verify` pass. Milestone 1 is
  complete. The Flutter release web build and Dart2Wasm dry-run also pass;
  milestone 2 device identity and encrypted local storage is next.
- 2026-08-24: Hosted CI run `32744891970` exposed that clean root Dart analysis
  incorrectly traversed the intentionally standalone Flutter packages before
  their dependencies were resolved. Root analysis now excludes the WampApp
  subtree, while a required Flutter 3.47.1 `WampApp Consumer` job runs the
  dedicated `bin/test-wamp-app` analysis, 17 tests, and release web build. The
  command passes locally. Run `32746938142` confirmed Fast Checks and WampApp,
  then exposed the same clean-checkout boundary in root formatting before Full
  Verify started. Root verification now formats tracked and untracked
  non-ignored workspace Dart files outside the subtree, and the dedicated
  command owns formatting for all three standalone packages. Exact-head run
  `32749847198` passed Fast Checks, WampApp Consumer, Full Verify, and Dart VM
  Coverage. The strict deployment auditor now recognizes WampApp Consumer as
  the fourth expected CI job. Exact-head run `32753979380` passed all four jobs
  for audit commit `bece5230`; strict CI cleanliness and hosted-log scanning
  passed without skipped, missing, unexpected, warning, deprecation, reset, or
  connection-noise findings.
- 2026-08-24: Milestone 2 is complete locally. The Flutter client now keeps
  Ed25519 and X25519 device keys in an account-and-endpoint-bound SecretBox
  vault derived through asynchronous Argon2id13, enrolls signed public device
  records through authenticated WAMP calls, supports durable revocation and
  safety-number verification, and seals and signs per-device conversation-key
  envelopes. The server derives device ownership exclusively from disclosed
  caller identity and persists no client private material. Trust completion is
  required before the controller reports a connected session, with stale
  reconnect and failed replacement results fenced. Repository `bin/verify` and
  `bin/test-wamp-app` pass; the focused command covers 9 shared, 13 server, and
  15 Flutter tests plus a release web build. Exact-head CI run `32761656169`
  passed Fast Checks, WampApp Consumer, Full Verify, and Dart VM Coverage for
  milestone commit `0a5c17c2`; the strict deployment audit passed exact-head
  job cleanliness and hosted-log scanning. Milestone 3 durable encrypted
  messaging is next.
- 2026-08-24: The milestone 3 direct-message foundation is complete locally.
  Binary XSalsa20-Poly1305 envelopes carry signed content-key wraps for every
  active sender and recipient device; caller-bound server procedures persist
  opaque mailbox records with idempotent identifiers, monotonic reconnect
  cursors, expiry filtering, and durable delivery/read receipt events. The
  Flutter client keeps its cursor and plaintext history inside the encrypted
  vault and exposes real compose, history, and sync controls. A two-account
  native-router test proves send, local decryption, acknowledgement, sender
  receipt sync, reconnect, and deduplication without plaintext in server
  storage. One-time messages fail closed pending true consumption semantics.
  `bin/test-wamp-app` and `bin/verify` pass with 14 shared, 21 server, and 19
  Flutter tests plus a release web build. Groups, explicit read and one-time
  UX, retry/conflict UX, and push-driven synchronization remain in milestone 3;
  commit `33ce907b` is pushed to GitLab and GitHub. Exact-head CI run
  `32767562794` passed Fast Checks, WampApp Consumer, Full Verify, and Dart VM
  Coverage. The strict deployment audit passed checked-out-head job cleanliness
  and hosted-log scanning without warning, deprecation, skip, reset, or
  connection-noise findings.
- 2026-08-24: Push-driven mailbox synchronization is complete locally. The
  server emits only a durable cursor on one exact WAMP topic and uses
  `eligible_authid` to restrict each event to message participants. The client
  subscribes before its initial snapshot, retains early cursors, coalesces
  concurrent events into serialized RPC sync, reports malformed events, and
  fences sign-out/replacement sessions. A three-account native-router test
  proves automatic message and receipt delivery while an unrelated account
  receives no event. `bin/test-wamp-app` and `bin/verify` pass with 16 shared,
  21 server, and 23 Flutter tests plus a release web build. Groups, explicit
  read and one-time UX, and retry/conflict UX remain in milestone 3. Commit
  `1ae4c3ea` is pushed to GitLab and GitHub. Exact-head CI run `32773529505`
  passed Fast Checks, WampApp Consumer, Full Verify, and Dart VM Coverage.
  Feature-branch exact-head job/log cleanliness and workflow/package visibility
  pass, and the separate strict audit confirms the protected `master` release
  policy.
- 2026-08-24: Explicit read state and atomic one-time consumption are complete
  locally. The consume RPC requires a message-specific Ed25519 proof from an
  active recipient device that held an original wrapped key, then serializes
  the first durable delivered/read/consumed cursor under the mailbox lock.
  Same-device retries are idempotent, competing devices and forged proofs fail
  closed, expiry cannot create new state, recipient reconnect does not
  resurrect consumed content, and controller lifecycle fences stop stale
  responses from revealing plaintext. Flutter exposes view-once and expiry
  controls while hiding one-time text before durable consumption. The native
  three-account smoke proves read and opened wakeups without unrelated event or
  plaintext leakage. `bin/test-wamp-app` passes with 19 shared, 26 server, and
  24 Flutter tests plus a release web build, and repository `bin/verify` passes
  on 2026-08-24. Groups and retry/conflict UX remain in milestone 3; hosted
  evidence is pending for this revision.
- 2026-08-25: Atomic encrypted group messaging is complete locally. One
  versioned envelope carries one authenticated ciphertext plus signed key wraps
  for every active participant device; the server validates one exact snapshot,
  appends one mailbox record, routes only to participants, and preserves
  independent recipient-authorized delivery/read state. The client persists
  immutable encrypted group descriptors, discovers them on reconnect, rejects
  metadata conflicts, aggregates sender receipts only after every recipient,
  and exposes group creation, selection, composition, expiry, and receipt UI.
  Group view-once remains deliberately invalid. The native router smoke proves
  three-participant delivery, independent reads, reconnect persistence,
  plaintext-free server storage, and outsider exclusion. `bin/test-wamp-app`
  passes with 23 shared, 29 server, and 26 Flutter tests plus a release web
  build, and repository `bin/verify` passes on 2026-08-25. Commit `84a28a4d`
  is pushed to GitLab and GitHub. Exact-head CI run `32786240083` passed all
  four required jobs, and feature-branch plus protected-`master` deployment
  audits passed.
- 2026-08-25: Milestone 3 is complete locally. The client persists one bounded
  encrypted outbox containing the exact encrypted envelope and optimistic local
  message before sending, classifies WAMP failures as retryable/rejected/
  conflict, reconciles lost replies before one reconnect retry, and fences late
  send results across sign-out. Exact mailbox matches converge without duplicate
  plaintext, while same-ID/different-envelope state fails closed as a terminal
  conflict. Message bubbles expose sending/syncing/failure state with explicit
  retry and discard controls. Vault encryption, outbox limits, mailbox
  disjointness, duplicate receipts, reconnect, conflict, and lifecycle races
  have focused regressions. `bin/test-wamp-app` passes with 23 shared, 29 server,
  and 40 Flutter tests plus a release web build; repository `bin/verify` passes
  on 2026-08-25. Milestone 4 encrypted rich content is next; hosted exact-head
  evidence is pending for this revision.
- 2026-08-25: The milestone 4 encrypted attachment foundation is complete
  locally. Direct and group messages can carry up to eight independently
  authenticated, 64 MiB attachments as 1 MiB binary WAMP chunks on the existing
  session. Upload-before-message ordering, exact retry/resume, recipient-scoped
  download, native-file/IndexedDB ciphertext persistence, local descriptor-key
  encryption, integrity verification, cancellation, stale-session fencing, and
  file/image/GIF picker/preview/save UX are covered. View-once attachments fail
  closed. `bin/test-wamp-app` passes with 28 shared, 36 server, and 50 Flutter
  tests plus a release web build; a real Chrome IndexedDB test and repository
  `bin/verify` also pass. A repeatable five-iteration 64 MiB benchmark reports
  0.159/0.139 Gbit/s for
  memory-cache encrypt/decrypt and 0.154/0.122 Gbit/s for durable disk on the
  current macOS host. Native/Web Worker crypto acceleration, server staging and
  retention quotas, in-app voice recording, and sticker/emoji selection remain
  before milestone 4 is complete. Exact-head CI run `32800458584` passed Fast
  Checks, WampApp Consumer, Full Verify, and Dart VM Coverage for commit
  `304edc8f`; the feature-head cleanliness/log audit and protected-`master`
  strict audit also pass.
- 2026-08-25: Server attachment quota and retention are complete locally.
  Configurable global/per-sender ciphertext limits are enforced atomically
  across concurrent uploads while exact duplicates remain idempotent at
  capacity. Manifest v2 timestamps migrate from v1, startup rebuilds usage and
  removes orphan bytes, and periodic staged-TTL cleanup uses unexpired mailbox
  references. Completeness verification and mailbox append share the mutation
  lock with pruning, covering both race orderings without dangling messages;
  commit also re-hashes stored ciphertext. The public WAMP boundary exposes a
  dedicated redacted quota error, and server shutdown drains cleanup. Focused
  restart, quota, expiry, corruption, and race tests plus a live router quota
  smoke pass. `bin/test-wamp-app` passes with 28 shared, 47 server, and 50
  Flutter tests plus a release web build; repository `bin/verify` passes.
  Native/Web Worker attachment crypto acceleration, in-app voice recording,
  and sticker/emoji selection remain before milestone 4 is complete.
- 2026-08-25: Native/Web Worker attachment crypto acceleration is complete
  locally. New descriptor v2 chunks use AES-256-GCM with descriptor and envelope
  metadata authenticated as AAD, while byte-compatible v1
  XSalsa20-Poly1305 attachments remain readable. Web Crypto runs exclusively in
  a dedicated Blob Worker with transferable buffers; native clients use the
  maintained `cryptography`/`cryptography_flutter` backend and its
  `BackgroundAesGcm` fallback when an OS plugin is unavailable. Cancellation,
  timeout, worker crash, disposal, malformed output, concurrency, stale-result
  isolation, and temporary-buffer clearing fail closed. Native and Chrome
  suites share an exact AES vector; Chrome also proves the event loop remains
  responsive during a 64 MiB operation. `bin/test-wamp-app` passes with 30
  shared, 47 server, 55 native Flutter, and 5 real-Chrome tests plus a release
  web build; repository `bin/verify` passes. Five 64 MiB fallback iterations
  measure 0.121/0.125 Gbit/s memory-cache encrypt/decrypt and 0.114/0.105
  Gbit/s durable-disk encrypt/decrypt. In-app voice recording and sticker/emoji
  selection remain before milestone 4 is complete; hosted exact-head evidence
  is pending.
- 2026-08-25: In-app voice notes are complete locally on the existing
  authenticated WAMP session and resumable E2EE attachment path. The client
  records bounded five-minute mono 16 kHz PCM16 streams, drains the final blocks
  during stop, emits a strictly validated WAV, and stores exact duration only in
  the encrypted descriptor. Permission denial, platform format adjustment,
  cancellation, timeout, stream failure, disposal, concurrency, and stale
  attempts fail closed. Playback validates the WAV before initialization, wipes
  in-memory sources, and releases the player before overwriting and deleting
  atomically unique temporary files where byte playback is not truly in-memory.
  Android/iOS/macOS microphone declarations and Linux runtime prerequisites are
  included. `bin/test-wamp-app` passes with 31 shared, 47 server, 68 native
  Flutter, and 11 Chrome tests plus Dart2Wasm and release-web builds.
  A macOS debug application build and repository-wide `bin/verify` also pass.
  Exact-head CI run `32814900960` passed all four required jobs for commit
  `23efec9d`; the feature-head cleanliness/log audit and protected-`master`
  strict audit pass. Sticker/emoji selection remains before milestone 4 is
  complete.
- 2026-08-25: Milestone 4 rich content is complete locally. The composer now
  provides searchable emoji insertion at the active selection and a bundled
  sticker picker. Stickers are rendered locally as valid bounded PNG payloads,
  retain `ChatAttachmentKind.sticker` through encryption, and use the existing
  resumable E2EE WAMP attachment path without another transport or server API.
  Rendering is serialized; intermediate RGBA and PNG scanline buffers and
  staged plaintext bytes are cleared defensively. The eight-attachment limit
  rejects a ninth sticker before rendering, and a horizontally bounded staged
  attachment strip keeps composer controls reachable at capacity.
  `bin/test-wamp-app` passes with 31 shared, 47 server, 72 native Flutter, and
  11 Chrome tests plus Dart2Wasm and release-web builds. All 8 widget tests pass
  explicitly in Chrome, a macOS debug application build passes, and
  repository-wide `bin/verify` passes. Milestone 5 user experience is next;
  exact-head CI run `32819793865` passed WampApp Consumer in 3m27s, Fast Checks
  in 8m33s, Full Verify in 11m13s, and Dart VM Coverage in 12m57s for commit
  `bdf2546c`. The feature-head cleanliness/log audit and protected-`master`
  policy/workflow/package strict audit pass.
- 2026-08-25: The first milestone 5 user-experience slice is complete locally.
  Authenticated members can read durable public profiles and edit only their
  own display name, single-line status, and bounded JPEG/PNG/WebP avatar.
  Profile RPCs use caller disclosure, CBOR binary avatar fields, optimistic
  revisions, serialized store updates, strict migration/validation, and
  client-side identity and stale-session fencing. The Flutter shell exposes
  profile editing, avatar selection/removal, recipient lookup, and an explicit
  authenticated-member visibility disclosure. `bin/test-wamp-app` passes with
  36 shared, 49 server, 76 native Flutter, and 11 real-Chrome tests plus
  Dart2Wasm and a release-web build. All 9 widget tests pass explicitly in
  Chrome, a macOS debug application build passes, and repository-wide
  `bin/verify` passes. Exact-head CI run `32825869072` passes WampApp Consumer
  in 3m26s, Fast Checks in 9m18s, Full Verify in 11m20s, and Dart VM Coverage
  in 12m52s for commit `a1af7d4d`; the feature-head cleanliness/log audit and
  protected-`master` policy/workflow/package strict audit pass. Contacts
  import, global search, read/unread filters, themes, per-chat mute, and
  platform push notifications remain in milestone 5.
- 2026-08-25: On-device global message search and received-message read/unread
  filters are complete locally. Search is case-insensitive, supports
  whitespace-separated AND terms across safe local conversation and attachment
  metadata, and returns the newest 200 matches in chronological order. Empty
  search remains scoped to the selected direct or group conversation; an active
  query spans local direct and group history. Search terms are neither sent nor
  persisted, hidden incoming view-once text is never indexed, and read filters
  explicitly exclude outgoing delivery state. Selecting a global result opens
  its conversation and restores the all-messages filter. Compact mobile account
  details plus horizontally scrollable conversation and composer controls keep
  the 390-pixel shell free of overflow. `bin/test-wamp-app` passes with 36
  shared, 49 server, 84 native Flutter, and 28 real-Chrome tests, followed by
  Dart2Wasm validation and a release-web build; a macOS debug application build
  and repository-wide `bin/verify` also pass. Exact-head CI run `32833791251`
  passes WampApp Consumer in 3m59s, Fast Checks in 10m31s, Full Verify in 11m4s,
  and Dart VM Coverage in 12m55s for commit `985afac1`; the feature-head
  cleanliness/log audit and protected-`master` policy/workflow/package strict
  audit pass. Contacts import, themes, per-chat mute, and platform push
  notifications remain in milestone 5.
- 2026-08-25: Account-bound appearance and per-chat notification preferences
  are complete locally. System, light, and dark themes plus bounded direct and
  group mute state are stored only inside the encrypted device vault. Missing
  preference documents migrate to safe defaults; malformed encrypted data,
  failed writes, concurrent mutations, and stale session completions fail
  closed. Muting affects only notification-presentation policy and never stops
  cursor wakeups or durable mailbox synchronization. Theme rebuilds preserve
  the active recipient and unsent composer draft, and compact widget coverage
  remains overflow-free. `bin/test-wamp-app` passes with 36 shared, 49 server,
  98 native Flutter, and 34 real-Chrome tests plus Dart2Wasm and a release-web
  build; repository-wide `bin/verify` passes. Contacts import and platform push
  delivery remain in milestone 5. Contacts import is deferred until account
  discovery and privacy semantics are defined.
