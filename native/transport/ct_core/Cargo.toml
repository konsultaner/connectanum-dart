[package]
name = "ct_core"
version = "0.1.0"
edition = "2021"

[dependencies]
socket2 = "0.5"
tokio = { version = "1.40.0", features = ["rt-multi-thread", "net", "sync", "time", "macros", "io-util"] }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
