[package]
name = "ct_ffi"
version = "0.1.0"
edition = "2021"

[dependencies]
ct_core = { path = "../ct_core" }
dashmap = "5.5"
tokio = { version = "1.40.0", features = ["rt", "rt-multi-thread", "macros", "time", "net"] }

[lib]
crate-type = ["cdylib", "rlib"]
