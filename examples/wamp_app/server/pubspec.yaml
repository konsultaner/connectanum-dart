name: wamp_app_server
description: Connectanum router and application services for the WampApp example.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  connectanum_client: 3.0.0-beta.5
  connectanum_core: 3.0.0-beta.5
  connectanum_router: 3.0.0-beta.5
  crypto: ^3.0.7
  googleapis_auth: ^2.3.3
  http: ^1.6.0
  path: ^1.9.0
  pinenacl: ^0.6.0
  wamp_app_protocol:
    path: ../shared
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
