# WampApp Protocol

This package defines the shared WAMP realms, procedure and error URIs, endpoint
validation, and account registration wire models used by the WampApp client and
server. It has no Flutter dependency.
