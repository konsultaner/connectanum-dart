name: Publish connectanum_auth_server to pub.dev

on:
  push:
    tags:
      - 'connectanum_auth_server-v*'

permissions:
  contents: read

jobs:
  validate:
    name: Validate connectanum_auth_server release tag
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v5

      - uses: dart-lang/setup-dart@v1

      - name: Validate package tag and archive
        run: |
          bin/validate-dart-package-publish-tag \
            connectanum_auth_server \
            packages/connectanum_auth_server
          bin/dart-package-publish-dry-run \
            --strict-release-ready \
            --show-release-plan \
            connectanum_auth_server

  publish:
    name: Publish connectanum_auth_server
    needs: validate
    permissions:
      contents: read
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/connectanum_auth_server
      environment: pub.dev
