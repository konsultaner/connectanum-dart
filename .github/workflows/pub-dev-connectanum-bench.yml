name: Publish connectanum_bench to pub.dev

on:
  push:
    tags:
      - 'connectanum_bench-v*'

permissions:
  contents: read

jobs:
  validate:
    name: Validate connectanum_bench release tag
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v5

      - uses: dart-lang/setup-dart@v1

      - name: Validate package tag and archive
        run: |
          bin/validate-dart-package-publish-tag \
            connectanum_bench \
            packages/connectanum_bench
          bin/dart-package-publish-dry-run \
            --strict-release-ready \
            --show-release-plan \
            connectanum_bench

  publish:
    name: Publish connectanum_bench
    needs: validate
    permissions:
      contents: read
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/connectanum_bench
      environment: pub.dev
